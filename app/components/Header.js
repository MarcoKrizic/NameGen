/*
* This is a JS component that renders the display for the header section of the HTML page.
*/

var React = require('react');
var ReactDOM = require('react-dom');

class Header extends React.Component {
    render() {
        return <p>CAPTAIN UNDERPANTS AND THE PERILOUS PLOT OF PROFESSOR POOPYPANTS NAME CHANGING GENERATOR</p>;
    }
}

module.exports = Header;